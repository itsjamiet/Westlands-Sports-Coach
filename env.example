# Copy this file to .env.local and fill in your real values.
# .env.local is git-ignored, so your key never gets committed.
#
# Both values come from: Supabase project -> Project Settings -> API
# Use the "anon" / "public" key here -- never the "service_role" key.

VITE_SUPABASE_URL=https://sbkcyuibgyzjvzgoqdda.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
